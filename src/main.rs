fn main() {}

/*
use std;

@derive(Debug);
type Foo(bar: i32);

fn Foo.add(self, rhs: i32) -> Self
    => Self(bar: self.bar + rhs);

fn main() -> ? => try {
    let foo = Foo(bar: 41) + 1;
    io::println(`{foo:?}`);
}
*/
